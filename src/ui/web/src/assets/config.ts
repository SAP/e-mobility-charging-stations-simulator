import type { BaseConfig } from '@/types/ConfigurationType';

const config: BaseConfig = {
  uiServer: {
    host: 'localhost',
    port: 8080,
    protocol: 'ui0.0.1',
  },
};

export default config;
