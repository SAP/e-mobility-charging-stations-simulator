export enum ChargingStationEvents {
  started = 'started',
  stopped = 'stopped',
  registered = 'registered',
  accepted = 'accepted',
  updated = 'updated',
  connectorStatusChanged = 'connectorStatusChanged',
}
