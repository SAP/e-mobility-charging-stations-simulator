export enum DBName {
  MARIA_DB = 'MariaDB',
  MONGO_DB = 'MongoDB',
  MYSQL = 'MySQL',
  SQLITE = 'SQLite',
}

export enum StorageType {
  JSON_FILE = 'jsonfile',
  MARIA_DB = 'mariadb',
  MONGO_DB = 'mongodb',
  MYSQL = 'mysql',
  NONE = 'none',
  SQLITE = 'sqlite',
}
